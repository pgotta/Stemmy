# ---- Stemmy v1.5 core requirements ----
#
# The tested Windows/RTX 50-series stack is installed by install_all.bat after
# these packages so audio-separator cannot leave a CPU-only or mismatched build:
#
#   torch==2.11.0+cu128
#   torchvision==0.26.0+cu128
#   torchaudio==2.11.0+cu128
#
# Do not add a bare torch/torchvision/torchaudio pin here. The correct wheel
# index depends on the GPU and platform.

Flask>=3.0

# Separation engine; includes onnxruntime-gpu and model download helpers.
audio-separator[gpu]>=0.18

# Tempo, key and beat analysis.
librosa>=0.10
soundfile>=0.12
numpy>=1.24
psutil>=5.9

# YouTube import and bundled FFmpeg helper.
yt-dlp
imageio-ffmpeg
